<div class="container">
  <div class="row">
    <div class="col-lg-10">
<h3>ログイン</h3>

<h5>会員の方はこちらからログイン</h5>

<%= form_with model:@customer,url: customer_session_path do |f| %>
　<label>メールアドレス</label>
  <div class="field">
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>
　<label>パスワード</label>
  <div class="field">
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン",class:"btn btn-info" %>
  </div>
<% end %>

<h6>会員登録がお済でない方</h6>

<p><%= link_to'こちら',new_customer_registration_path %>から新規登録を下さい。</p>

    </div>
  </div>
</div>
